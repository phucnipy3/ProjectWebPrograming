@model MVC.Areas.Admin.Models.DeleteProductDetailViewModel
@using (Html.BeginForm("Delete", "Products", FormMethod.Post, new { id = "formConfirmDelete"}))
{
    <div class="row">
        <div class="col-auto mx-auto">
            <p class="text-center">
                <h5>Bạn có chắc chắn muốn xóa?</h5>
                @Html.TextBoxFor(model => model.ProductID, new { hidden = true })
                @Html.TextBoxFor(model => model.ProductID, new { hidden = true })
            </p>
        </div>
    </div>
    <div class="row">
        <div class="col-auto mx-auto">
            <button type="submit" class="btn btn-primary ">Yes</button>
            <button type="button" class="btn btn-secondary " data-dismiss="modal">No</button>
        </div>
    </div>
}
